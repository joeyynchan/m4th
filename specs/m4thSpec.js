describe('M4th', function() {

  describe('gcd', function() {

    describe('gcd between two positive integers', function() {

      it('GCD(0, 0) = 0', function() {
        expect(gcd(0, 0)).toEqual(0);
      })

    });

  });


});
